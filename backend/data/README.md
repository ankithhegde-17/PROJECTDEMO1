# Mock HR data

Seed fixtures are defined in `app/seed.py` so a clean SQLite database is reproducible. They include eight employees, August attendance and overtime signals, Q3 performance records, and a six-candidate hiring funnel.
